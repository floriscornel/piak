<?php
{{#if imports}}

{{#each imports}}
use {{this}};
{{/each}}
{{/if}}
{{#if namespace}}

namespace {{namespace}};
{{/if}}

readonly class {{className}} {
    {{#each properties}}
    {{#if isArray}}
    /**
     * @var {{arrayType}}[]
     */
    {{/if}}
    public {{type}} ${{name}};
    {{/each}}

    public function __construct(
        {{#each properties}}
        public {{type}} ${{name}}{{#unless @last}},{{/unless}}
        {{/each}}
    ) {}
}